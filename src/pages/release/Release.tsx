import { Box } from '@chakra-ui/react';

import ReleasePage from '@/ui/release/index';

type Props = {};

const Release = (props: Props) => {
  return (
    <Box>
      <ReleasePage />
    </Box>
  );
};

export default Release;
